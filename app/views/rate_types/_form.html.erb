<%= form_for(@rate_type) do |f| %>
  <% if @rate_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rate_type.errors.count, "error") %> prohibited this rate_type from being saved:</h2>

      <ul>
      <% @rate_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :nb_month %><br />
    <%= f.text_field :nb_month %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
